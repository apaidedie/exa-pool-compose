services:
  exa-pool:
    image: ghcr.io/apaidedie/exa-pool-compose:latest
    container_name: exa-pool
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
